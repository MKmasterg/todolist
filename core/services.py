
def is_project_name_existing(name: str) -> bool:
    """Check if a project name already exists in the database.
    :param name: The project name to check.
    :return: True if the name exists, False otherwise.
    """

    # Placeholder for database check logic
    existing_project_names = ["Existing Project 1", "Existing Project 2"]  # Example existing names
    return name in existing_project_names